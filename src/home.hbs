{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}
<script>
  $(document).ready(function () {
    $("#blog-post").hover(function () {
      $("#blog-post p").toggleClass("blog-post-hover");
    });
  });
</script>
{{!-- The main content area --}}
<main id="site-main">
  <div class="mt-0.5 mr-[1.8rem] font-sans">
    <div class="flex flex-col">
      {{#page}}
        <div class="flex flex-col ml-4 mt-8 mb-2">
          <h1 class="text-[4rem] leading-[3.5rem] ml-[-0.2rem] mb-[0.25rem]">{{title}}</h1>
          {{content}}
        </div>
              {{#if feature_image}}
              {{!-- This is a responsive image, it loads different sizes depending on device
              https://medium.freecodecamp.org/a-guide-to-responsive-images-with-ready-to-use-templates-c400bd65c433 --}}
                <img class="flex flex-grow-1 object-cover w-full h-full ml-4"
                     srcset="{{img_url feature_image size="s"}} 300w,
                    {{img_url feature_image size="m"}} 600w,
                    {{img_url feature_image size="l"}} 1000w,
                    {{img_url feature_image size="xl"}} 2000w"
                     sizes="100vw"
                     src="{{img_url feature_image size="m"}}"
                     alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
                     loading="lazy"
                />
              {{/if}}
      {{/page}}



    </div>
  </div>
</main>
